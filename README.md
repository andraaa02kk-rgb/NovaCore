# NOVA//CORE
Premium Expo project.

## Termux
cd ~/NOVA-CORE
npm install
npx expo start

Open the QR in Expo Go.

The project uses public Expo APIs and displays N/A for privileged telemetry that Expo Go cannot provide. Native Development Build is required for features such as persistent background media, system overlays, and richer Android thermal/performance telemetry.
